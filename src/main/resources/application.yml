spring:
  application:
    name: myapp
  profiles:
    active: dev # 기본프로필
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect